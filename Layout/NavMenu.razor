<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">simpleReading</a>
        <button class="disableButton" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>@menu
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="adicionar">
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Adicionar nova leitura
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="baixar">
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Salvar/Baixar leituras
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="remover">
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Remover leitura
            </NavLink>
        </div>
                <div class="nav-item px-3">
            <NavLink class="nav-link" href="atualizar">
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Atualizar leitura
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private static bool collapseNavMenu = true;

    private static string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private string menu => collapseNavMenu ? "abrir menu" : "fechar menu";

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
